# This is a Databricks asset bundle definition for databricks_cityride.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: databricks_cityride
  uuid: f7d8f1ed-0d42-43c5-aa24-b9626a86de96

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405608667705126.6.azuredatabricks.net
    variables:
      catalog: citybike_dev
      schema: ${workspace.current_user.short_name}

  test:
    mode: production
    workspace:
      host: https://adb-7405617909562397.17.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: citybike_test
      schema: prod
    permissions:
      - user_name: shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com
        level: CAN_MANAGE

  prod:
    mode: production
    workspace:
      host: https://adb-7405612008039338.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Users/shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: citybike_prod
      schema: prod
    permissions:
      - user_name: shantanunarale061_gmail.com#ext#@shantanunarale061gmail.onmicrosoft.com
        level: CAN_MANAGE
